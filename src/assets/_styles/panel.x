standard = {
	stroke    = F(2); 
	style     = S(FILL);
	fill      = C(10,10,10);
	other     = C(100,100,100);
	outline   = C(0,0,0);
	gradient  = G(HORTIZONTAL);
};

hover(standard) = {
};

pressed(hover) = {
};

