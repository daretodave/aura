standard = {
	text_size  = F(12);
	wrapped    = B(true);
	h_align    = A(MIDDLE);
	v_align    = A(MIDDLE);
	fill       = C(200,200,200);
};

hover(standard) = {
};

pressed(hover) = {
};

hint(standard) = {
	fill = C(100,100,100);
};